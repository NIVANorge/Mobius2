
model("Simple test model") {
	
	extend("simplyq_model.txt")
	
	md : module("So very simple", 1, 1, 0) {
		
		soil : compartment("Soil")
		
		water : quantity("water")
		oc    : quantity("organic carbon")
		tox   : quantity("toxin")
		
		soil.has(oc, unit(), "SOC") .initial { 500 }
		soil.water.has(oc, unit(), "DOC")
		
		soil.water.has(tox, unit(), "Truly dissolved toxin")
		soil.oc.has(tox, unit(), "Adsorbed toxin")
		soil.water.oc.has(tox, unit(), "DOC-bound toxin")
		
		#TODO: finish this!
	
	}
	
	sol : solver("solver", "INCADascru", 0.1)

}
