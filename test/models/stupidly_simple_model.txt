
model("Simple test model") {
	
	a : compartment("A")
	q : property("Q")
	
	module("So very simple", version(1, 1, 0)) {
	
		par_group("Test") {
			b : par_bool("Check this!", true)
		}
		
		var(a.q, [], "A Q") {
			(-1)^(time.step=>[])
		}
	}
}
