

model("Another simple") {

	sp : module("So simple", 1, 0, 0) {
	
		a : compartment("A")
		q : quantity("Q")
		
		a.has(q, unit(), "QA") .initial {
			b
		}
		
		a.par_group("Pars") {
			b : par_real("test", unit(), 0)
		}
		
		a.has(c : property("test")) {
			b * sin(time.day_of_year * 2.0 * pi / 365.0)
		}
	}
	
	sw : module("Another simple", 1, 0, 0) {
		
		a : compartment("A")
		q : quantity("Q")
		
		flux(a.q, nowhere, "The flux") { 5 }
		
	}
	
	a.compartment("A")
}