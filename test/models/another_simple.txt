

model("Another simple") {

	a : compartment("A")
	
	module("So simple", version(2, 0, 0)) {

		r : property("R")
		s : property("S")
		t : property("T")
		u : property("U")
		
		var(a.u, []) { r } # To show that it doesn't report this as a part of the cycle.
		var(a.r, []) { t }
		var(a.s, []) { r }
		var(a.t, []) { s }
	}
}