

model("Another simple") {

	a : compartment("A")
	p : property("P")
	k : index_set("K")
	
	distribute(a, k)
	
	module("So simple", version(2, 0, 0)) {

		par_group("The Q", a) {
			q : par_real("Q", [], 0)
		}
		
		var(a.p, [], "A P") {
			q
		}
	}
}