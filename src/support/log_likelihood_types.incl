// The third argument is the number of error parameters to the residual distribution.

SET_LOG_LIKELIHOOD(normal, "Normal", 1)
SET_LOG_LIKELIHOOD(normal_h, "Normal hetereoskedastic", 1)
SET_LOG_LIKELIHOOD(wls, "WLS", 2)
SET_LOG_LIKELIHOOD(wlsar1, "WLS-AR1", 3)