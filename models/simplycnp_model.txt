	
model("SimplyCNP") {

	extend("simplyc_model.txt")
	extend("simplyn_model.txt")
	extend("simplyp_model.txt")

	on   : quantity("Organic nitrogen")
	toc  : property("Total organic carbon")
	
	load("modules/simplyn.txt",
		module("Simply organic C-N", river, water, on, oc)
	)
	
	load("modules/simplyc.txt",
		module("Simple river TOC", river, water, oc, sed, toc, sed_oc)
	)
	
	par_group("Suspended carbon") {
		sed_oc : par_real("Suspended sediments OC fraction", [], 0.02, 0, 1)
	}
}