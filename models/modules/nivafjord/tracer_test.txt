
module("NIVAFjord tracer test", version(0, 0, 1),
	layer  : compartment,
	bnd_layer : compartment,
	river  : compartment,
	water  : quantity,
	tracer : quantity
) {

	var(layer.water.tracer, [k g])
	
	var(bnd_layer.water.tracer, [k g]) @override_conc { 0.2[k g, m-3] }
	
	var(river.water.tracer, [k g]) @override_conc { 1[k g, m-3] }
}