
module("NIVAFjord tracer test", version(0, 0, 1),
	layer  : compartment,
	river  : compartment,
	water  : quantity,
	tracer : quantity,
	horz   : connection
) {

	var(layer.water.tracer, [k g])
	
	var(river.water.tracer, [k g]) @override_conc { 1[k g, m-3] }
	
	flux(out, layer.water.tracer, [k g, day-1], "Horizontal flux tracer equivalent removal") {
		-in_flux(horz, layer.water)*conc(tracer)->>
	}
}